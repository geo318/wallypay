'use client'

import React from 'react'
import { Next13ProgressBar, Next13ProgressProps } from 'next13-progressbar'

export const ProgressBar = (props: Next13ProgressProps) => {
  return <Next13ProgressBar {...props} />
}
